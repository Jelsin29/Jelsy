import { describe, it } from "vitest"

describe("defaultReporter", () => {
  it.todo("formats errors as a table")
})

describe("formatReportTable", () => {
  it.todo("produces correct table format")
})
