// Benchmark: build, gzip, measure, compare against 3KB threshold
// Target: < 3KB min+gzip (full), < 1KB (minimal)

// TODO: implement benchmark
console.log("bundle-size benchmark — not yet implemented")
